name: "Run CI"

on:
  pull_request:
    types: [opened, reopened, synchronize]
  merge_group:
  push:
    branches: ['release/**']

permissions: {}

jobs:
  code-checks:
    permissions:
      contents: read
    uses: ./.github/workflows/quality.yml

